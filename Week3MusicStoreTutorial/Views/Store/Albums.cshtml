@model IEnumerable<Week3_MusicStoreTutorial.Models.Album>

@{
    ViewData["Title"] = "Albums";
}

<h2>Albums</h2>

<ul>
    @foreach (var album in Model)
    {
        <li>@album.Name</li>
    }

</ul>
